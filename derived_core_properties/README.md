The `DerivedCoreProperties.txt` parser used by `unicode-id-trie-rle`. This
crate is considered an implementation detail of `unicode-id-trie-rle` and makes
no guarantees about stability or correctness.
